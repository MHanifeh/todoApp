# Hello World Python Script

This is a simple Python script that prints "Hello, World!".

## How to run

Use the configured Python virtual environment:

```bash
/Users/mahdi/Desktop/new-test/.venv/bin/python main.py
```

Or simply run `python main.py` if the environment is activated.